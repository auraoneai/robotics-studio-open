# Robotics Studio Open Release Blockers

Date: 2026-05-13

These blockers are intentionally unchecked until the external dependency is
completed and independently verified.

| Area | Blocker | Owner | Exact next action | Verification |
|---|---|---|---|---|
| Legal | MIT license review for desktop shell not approved. | Legal | Review `LICENSE`, dependency license policy, and Tauri shell third-party notices. | Legal approval recorded in release issue. |
| Trademark | "Robotics Studio" USPTO/EUIPO clearance not complete. | Legal | Run USPTO and EUIPO search; record clearance memo. | Clearance memo linked from launch checklist. |
| Sample dataset IP | SO-101 kitchen sample license not approved. | Legal + Eng | Confirm synthetic provenance or license grant; publish data license. | Dataset card and license in HF repo. |
| Security | External security review not complete. | Security | Engage reviewer for PyO3, ROS bag parser, video decoder, updater, and install script review. | Signed report with remediation status. |
| Signing | Apple Developer ID and Windows EV cert access not available in local env. | Platform owner | Provision org secrets `APPLE_ID`, `APPLE_TEAM_ID`, `APPLE_APP_SPECIFIC_PASSWORD`, `WINDOWS_EV_CERT_THUMBPRINT`. | Signed DMG/MSI in GitHub release. |
| Notarization | macOS notarization cannot run without Apple credentials. | Platform owner | Run `.github/workflows/release.yml` on macOS with secrets. | `notarytool` success log. |
| PyPI | `robostudio-engine` package reservation requires PyPI trusted publisher or token. | Release owner | Configure PyPI project `robostudio-engine` trusted publisher for `auraoneai/robotics-studio-open`. | PyPI project page and dry-run publish log. |
| Homebrew | AuraOne Homebrew tap submission requires tap repo write access. | Release owner | Add `robotics-studio-open.rb` cask to `auraoneai/homebrew-open`. | `brew audit --cask robotics-studio-open` and tap PR/commit. |
| DNS | `robotics-studio.auraone.ai` and `install.auraone.ai/robotics-studio` require DNS/CDN control. | Infra owner | Apply DNS records from `opensource/open-studio-platform/distribution/dns/robotics-studio-open.zone`. | `dig` and HTTPS 200 checks. |
| Hugging Face | `auraoneai` HF org sample dataset space not provisioned. | Release owner | Create `auraoneai/so101-kitchen-sample` and publish dataset card/license. | HF dataset URL resolves. |
| CI | 14 days green cannot be asserted for new repo. | Release owner | Enable required workflows and observe 14 consecutive days of green main. | Branch protection history. |
| Accessibility | Final renderer audit cannot run until app UI lands. | QA | Run `scripts/verify-accessibility.sh` after Agent 3 renderer lands. | Axe/Playwright artifact. |
| Performance | Full baseline cannot run until app and fixture dataset land. | QA | Run `scripts/verify-performance-baseline.sh` after Agent 3/4 land app/engine. | Baseline artifact in release. |
| Launch | Newsletter, HN, X, podcast outreach, design partner quotes require human approvals. | Launch owner | Execute `docs/release/launch-checklist-2026-05-13.md`. | Scheduled posts/emails and launch issue evidence. |
