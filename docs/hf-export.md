# Hugging Face Export Guide

Hugging Face export writes a reviewed subset manifest, dataset card, QA report, failure cluster manifest, and embodiment card. Tokens are stored through the Open Studio Platform keychain API. The CLI can create a dry-run upload manifest without credentials.
